# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# AWS credentials / secrets — never commit
.aws/
credentials
*.pem
*.key
.env
.env.*

# Local Claude Code settings
.claude/

# Runtime state written by register.py (contains live ARNs) — not for the repo
.agentcore_state.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
